Busybox Container with Curl
===========================

This is just an extra layer on top of the `busybox` image that adds a
[curl binary](http://www.magicermine.com/demos/curl/curl/curl.html) to
`/usr/bin`.

To build just run `make`.
